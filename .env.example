# =============================================================================
# investment-prediction-service 환경 변수
# =============================================================================
# backend는 이 서비스를 AI_PREDICTION_SERVICE_URL (기본 http://localhost:8000) 로 호출.
# .env 파일은 저장소에 커밋하지 않음.
# =============================================================================

# ----- CORS (쉼표 구분 허용 오리진, 미설정 시 *) -----
CORS_ORIGINS=*

# ----- 모델 경로 (예측 시 로드할 LSTM 모델 파일) -----
# MODEL_PATH=./models/lstm_model.pt
# LSTM_MODEL_PATH=./models/lstm_model.pt
